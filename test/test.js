/*jslint node: true */
/*globals test, suite, setup */

'use strict';

var chai = require('chai'),
    assert = chai.assert,
    dc = require('../dust-compiler.js');


suite('dust-compiler-tests', function () {
    test('test something', function () {
        assert.strictEqual(false, true);
    });

});
